"use client";

import { Address } from "@/interfaces";

interface RestaurantLocationProps {
	address: Address;
}

const RestaurantLocation = ({ address }: RestaurantLocationProps) => {
	return <div>RestaurantLocation</div>;
};

export default RestaurantLocation;
